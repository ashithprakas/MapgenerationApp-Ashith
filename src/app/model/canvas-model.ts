export interface CanvasModel {
  canvasState: string;
  canvasActionType: string;
}

export interface SetPropertiesModel {
  StrokeWidth: string;
  StrokeColor: string;
  FillColor: string;
  ObjectAngle: string;
}
